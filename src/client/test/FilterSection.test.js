import React from 'react';
import { shallow } from 'enzyme';
import  FilterSection from '../components/FilterSection';

describe("MyComponent", () => {
  it("should render correctly", () => {
    const component = shallow(<FilterSection/>);
  });
  
});